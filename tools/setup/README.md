# Project setup

Create the virtual environment and install Python dependencies from the project root.

Windows:

```batch
tools\setup\setup.bat
```

Linux or macOS:

```bash
chmod +x tools/setup/setup.sh
./tools/setup/setup.sh
```

FFmpeg has a separate installer in `tools/ffmpeg`.
